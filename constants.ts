
export const NUM_WIRES = 10;
export const BEADS_PER_WIRE = 10;
export const INITIAL_BEADS_ON_LEFT = 0; // All beads start on the right

// Tailwind CSS unit numbers (e.g., w-6, h-6 for 1.5rem or 24px by default)
export const BEAD_SIZE_TW_UNIT = 6; // Represents 1.5rem (24px)
export const GAP_WIDTH_TW_UNIT = 20; // Represents 5rem (80px), for the gap between bead groups

export const MAX_HISTORY_SIZE = 30; // Max undo steps